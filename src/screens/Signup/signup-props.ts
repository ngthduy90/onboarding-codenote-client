import { RouteComponentProps } from "react-router";
import { AuthenticateProps } from "../../types/authenticate-type";

export interface SignupProps extends AuthenticateProps, RouteComponentProps {

}